import React from 'react';

import { Header } from '@components/shared';
import LookupPage from '@components/lookup/Lookup';

const Lookup: React.FC = () => (
  <>
    <Header />
    <LookupPage />
  </>
);

export default Lookup;
